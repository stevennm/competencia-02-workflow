# Python dependencies for VERSION_WORKFLOW_JUEVES
# Install in VM with: pip install -r requirements.txt
#
# NOTE: lightgbm is NOT included here because we use zLightGBM
# which must be installed separately following the professor's instructions

polars>=0.19.0
numpy>=1.24.0
optuna>=3.0.0
tqdm>=4.65.0
pyarrow>=22.0.0
duckdb>=0.9.0
matplotlib>=3.10.7
altair>=5.5.0
scikit-learn>=1.3.0
optuna-dashboard>=0.19.0
matplotlib-venn>=1.1.2
